This is a mongodb project. That incorporates modules such as mongoose as ORM, mocha as a testing framework and nodemon.

Instructions:
 - to create the package.json dependency (inside the project directory): npm init
 - to install the nodemon, mocha and mongoose depencies: npm install --save nodemon   mocha mongoose
 - to start the server just use: node test\test_helper.js